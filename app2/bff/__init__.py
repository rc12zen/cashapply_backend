# BFF (Backend-for-Frontend) layer — all UI-facing API routes and their service logic.
# Completely isolated from the core processing engine (extraction, rule_engine).
# Adding/changing a UI endpoint never touches Phase 1/2/3 code.
